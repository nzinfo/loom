version: loom-schema/v2
entity: entity:shop.core.Product
group: pricing
using:
  - shop.core.*
fields:
  - name: discount_rate
    type:
      ref: decimal
      args: { precision: 5, scale: 2 }
  - name: tier_price
    type: shop.core.Money
