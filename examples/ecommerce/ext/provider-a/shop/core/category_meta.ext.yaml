version: loom-schema/v2
entity: entity:shop.core.Category
strategy: sidecar_jsonb
table: products_ext
group: meta
using:
  - shop.core.*
fields:
  - name: display_order
    type: integer
  - name: icon_url
    type:
      ref: string
      args: { max_length: 512 }
