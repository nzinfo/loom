version: loom-schema/v2
entity: entity:shop.core.Product
strategy: sidecar_jsonb
group: acme_internal
using:
  - shop.core.*
fields:
  - name: acme_sku
    type:
      ref: string
      args: { max_length: 48 }
  - name: acme_cost_center
    type:
      ref: string
      args: { max_length: 16 }
