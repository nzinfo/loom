{
  "name": "@loom/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "loom": "./bin/loom"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@loom/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
