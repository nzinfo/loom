# loom v2 统一类型系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 loom-schema 从 v1（`base`/`ref` 互斥 + 带 kind 前缀的类型引用）bump 到 v2（单一 `type:` 键 + 编程语言式 `using` 导入机制），消除 v1 的类型论语义错误。

**Architecture:** 字段从 `base`/`ref` 两个互斥键改为单一 `type:` 键，值是单段名（base_types 短名）或三段名（value_type 节点，不带 kind 前缀——被引用节点自身的 `kind: value_type` 已经声明"我是类型"）。新增文件级 `using:` 导入机制（默认 `base.core.*`），让 base_types 标量与 value_type 在同一类型名字空间内地位平等。base_types 获得模块身份 `base.core`，标量全限定名为 `base.core.integer` 等。enum 强制走 value_type，取消 inline enum。

**Tech Stack:** TypeScript（ESM、NodeNext、strict、`verbatimModuleSyntax`）、Zod 3、yaml 2、Vitest 2、Biome 1.9、pnpm 工作区。@loom/core 环境无关（无 `node:fs`、无 `Buffer`、无 `!` 非空断言）；@loom/cli 是 Node 包装。

**Spec:** `docs/specs/2026-06-18-loom-v2-type-system.md`
**Branch:** `feat/v2-type-system`（已开，从 `feat/loom-v1` 干净切出）

---

## 文件结构总览

要新建 / 修改的文件：

### 新建
- `packages/core/src/ir/typespace.ts` — 类型名字空间解析器（核心新模块）
- `packages/core/tests/typespace.test.ts` — 类型名字空间单元测试
- `packages/core/tests/fixtures/v2_schema.ts` — v2 语法的完整固件（替代 v1 的 base_schema.ts，但 v1 固件先保留共存至最后清理任务）

### 修改 — core
- `packages/core/src/ir/version.ts` — `FORMAT_VERSION` v1 → v2
- `packages/core/src/ir/schemas.ts` — field schema 重写（`base`/`ref` → `type`）；新增 `using` 字段；移除 inline enum 分支
- `packages/core/src/ir/refs.ts` — 新增三段名类型引用解析（无 kind）；保留身份 ref（带 kind）用于 mixin include / primary_table / ref_table
- `packages/core/src/loader/link.ts` — 引入 typespace 解析；`ref` 字段解析改为 `type` 解析；mixin include 不变
- `packages/core/src/loader/validate.ts` — `checkScalarField` → `checkTypedField`，基于 type 解析结果校验
- `packages/core/src/projector/expand.ts` — `expandField` 从 base/ref 分支改为单一 type 分支
- `packages/core/src/index.ts` — 导出 typespace 模块
- `packages/core/tests/fixtures/base_schema.ts` — 固件改 v2 语法（保留函数签名不变）
- `packages/core/tests/golden/base_schema.pg.sql` — 重新生成（语义等价）
- 所有 `packages/core/tests/*.test.ts` — 测试用例改 v2 语法

### 修改 — cli
- `packages/cli/src/commands/version.ts` — 无需改（`FORMAT_VERSION` 引用自动跟随）

### 修改 — 文档
- `docs/USER_GUIDE.md` — 第 5/10/11 章重写（value_type / enum / ref → type + using）
- `README.md` — Status 段 v0.1.0 → v0.2.0

---

## 任务列表

- Task 1: version bump + 文件头校验
- Task 2: field schema 重写为单一 `type:` 键
- Task 3: 三段名类型引用解析器（ir/typespace.ts 骨架）
- Task 4: `using:` 字段加入 ParsedFileBase + Zod schema
- Task 5: 类型名字空间解析（default `base.core.*` + 文件 using + 短名歧义检测）
- Task 6: link 阶段集成 typespace（替换 v1 的 ref 解析）
- Task 7: validate 阶段重写（checkTypedField）
- Task 8: enum 强制走 value_type（移除 inline enum 校验路径）
- Task 9: projector expand.ts 改造（单一 type 分支）
- Task 10: 测试固件迁移到 v2 语法（base_schema.ts）
- Task 11: 所有 *.test.ts 迁移到 v2 语法
- Task 12: 黄金固件重新生成（base_schema.pg.sql）
- Task 13: cli version 命令输出 v2
- Task 14: USER_GUIDE + README 更新
- Task 15: 全量 lint + 完整测试 + 最终提交 + 标签 v0.2.0

---

## Task 1: version bump + 文件头校验

**Files:**
- Modify: `packages/core/src/ir/version.ts:14-18`
- Modify: `packages/core/src/ir/version.ts:20` (注释 v1 → v2)
- Test: `packages/core/tests/version.test.ts`

**背景：** v1 的 `FORMAT_VERSION = 'v1'`。v2 是破坏性 wire 格式变更，必须 bump。`CURRENT_VERSION` 派生自 `FORMAT_FAMILY/FORMAT_VERSION`，会自动变成 `loom-schema/v2`。`parseFile` 已经严格匹配 `CURRENT_VERSION`，所以这一改后所有 v1 文件会立即报 `version` 错误——这是预期行为，为后续任务提供安全网。

- [ ] **Step 1: 先看现有测试断言**

Run: `cat packages/core/tests/version.test.ts`
Expected: 看到对 `CURRENT_VERSION === 'loom-schema/v1'` 的断言。记录该断言用于 Step 3 修改。

- [ ] **Step 2: 改 version.ts 的常量**

修改 `packages/core/src/ir/version.ts`：

把
```typescript
/** Current wire version. Breaking changes must bump this. */
export const FORMAT_VERSION = 'v1' as const;
```
改为
```typescript
/** Current wire version. Breaking changes must bump this. */
export const FORMAT_VERSION = 'v2' as const;
```

把
```typescript
/** All file kinds recognized by loom-schema/v1. See spec §9. */
```
改为
```typescript
/** All file kinds recognized by loom-schema/v2. See spec §9. */
```

`CURRENT_VERSION` 自动变成 `'loom-schema/v2'`，不需要改。

- [ ] **Step 3: 改测试断言**

修改 `packages/core/tests/version.test.ts`，把所有 `'loom-schema/v1'` 替换为 `'loom-schema/v2'`。

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- version.test.ts`
Expected: PASS（version.test.ts 通过）。

注意：此刻其他测试会大量失败（因为固件还写 v1）。**不要现在跑全量测试**。只跑 version.test.ts 确认本任务通过。

- [ ] **Step 5: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 6: 提交**

```bash
git add packages/core/src/ir/version.ts packages/core/tests/version.test.ts
git commit -m "feat(v2): bump FORMAT_VERSION v1 → v2

Wire-format breaking change. CURRENT_VERSION is now loom-schema/v2.
parseFile already strictly matches CURRENT_VERSION, so v1 files will
emit 'version' diagnostics — this is the safety net for the migration
tasks that follow."
```

---

## Task 2: field schema 重写为单一 `type:` 键

**Files:**
- Modify: `packages/core/src/ir/schemas.ts:45-74` (baseField / refField / fieldOrInclude)
- Test: `packages/core/tests/schemas.test.ts`

**背景：** v1 schemas.ts 定义了 `baseField`（含 `base:`）和 `refField`（含 `ref:`），通过 `fieldOrInclude` union + superRefine 强制互斥。v2 改为单一 `typeField`（含 `type:`，值是字符串），保留 `includeEntry` 不变（mixin include 仍走独立语法，见 spec §5）。

`type:` 的值是字符串，不做形态校验（单段 vs 三段）——形态分流在 typespace 解析器（Task 3）做。Zod 这里只保证"存在、非空字符串"。

- [ ] **Step 1: 写失败测试 — type 字段接受单段名**

在 `packages/core/tests/schemas.test.ts` 顶部 import 区后追加：

```typescript
import { parseFile } from '../../src/ir/schemas.js';

describe('v2 field schema', () => {
  it('accepts a field with type: <single-segment>', () => {
    const yaml = `version: loom-schema/v2
kind: mixin
name: M
fields:
  - name: age
    type: integer
`;
    const f = parseFile(yaml, 'test.yaml');
    expect(f.kind).toBe('mixin');
    const fields = (f.data as { fields: Array<Record<string, unknown>> }).fields;
    expect(fields[0]?.type).toBe('integer');
    expect((fields[0] as Record<string, unknown>).base).toBeUndefined();
    expect((fields[0] as Record<string, unknown>).ref).toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: FAIL（`fields[0].type` 为 undefined，因为 v1 schema 要求 `base` 或 `ref`）。

- [ ] **Step 3: 写失败测试 — type 字段接受三段名**

在同一 describe 块追加：

```typescript
  it('accepts a field with type: <three-segment>', () => {
    const yaml = `version: loom-schema/v2
kind: mixin
name: M
fields:
  - name: email
    type: base.core.Email
`;
    const f = parseFile(yaml, 'test.yaml');
    const fields = (f.data as { fields: Array<Record<string, unknown>> }).fields;
    expect(fields[0]?.type).toBe('base.core.Email');
  });
```

- [ ] **Step 4: 跑测试再次失败**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: 两条新测试都 FAIL。

- [ ] **Step 5: 写失败测试 — 拒绝 base 字段**

追加：

```typescript
  it('rejects a field with the v1 base: key', () => {
    const yaml = `version: loom-schema/v2
kind: mixin
name: M
fields:
  - name: age
    base: integer
`;
    expect(() => parseFile(yaml, 'test.yaml')).toThrow();
  });
```

- [ ] **Step 6: 写失败测试 — 拒绝 ref 字段**

追加：

```typescript
  it('rejects a field with the v1 ref: key', () => {
    const yaml = `version: loom-schema/v2
kind: mixin
name: M
fields:
  - name: email
    ref: value_type:base.core.Email
`;
    expect(() => parseFile(yaml, 'test.yaml')).toThrow();
  });
```

- [ ] **Step 7: 跑测试再次失败**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: 4 条新测试都 FAIL。

- [ ] **Step 8: 改 schemas.ts — 用 typeField 替换 baseField/refField**

修改 `packages/core/src/ir/schemas.ts`。

删除（行 45-74）：
```typescript
const baseField = z
  .object({
    name: z.string().min(1),
    base: z.string().min(1),
    required: z.boolean().optional(),
    unique: z.boolean().optional(),
    default: z.unknown().optional(),
  })
  .catchall(z.unknown());

const refField = z
  .object({
    name: z.string().min(1),
    ref: z.string().min(1),
    required: z.boolean().optional(),
    unique: z.boolean().optional(),
    default: z.unknown().optional(),
  })
  .catchall(z.unknown());

const includeEntry = z.object({ include: z.string().min(1) }).strict();

const fieldOrInclude = z.union([baseField, refField, includeEntry]).superRefine((val, ctx) => {
  if ('base' in val && 'ref' in val) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      message: 'field cannot have both base and ref (spec §10)',
    });
  }
});
```

替换为：
```typescript
/**
 * A field with a type reference (spec v2 §3).
 *
 * `type:` is the single key. Value is a type name — single-segment
 * (base_types short name, e.g. "integer") or three-segment (value_type
 * node, e.g. "base.core.Email"). Form discrimination happens in the
 * typespace resolver, not here.
 *
 * `catchall` allows scalar-specific properties (max_length, precision,
 * scale, values, ...) to pass through; validation against base_types
 * property schemas happens in Pass 3.
 */
const typeField = z
  .object({
    name: z.string().min(1),
    type: z.string().min(1),
    required: z.boolean().optional(),
    unique: z.boolean().optional(),
    default: z.unknown().optional(),
  })
  .catchall(z.unknown());

const includeEntry = z.object({ include: z.string().min(1) }).strict();

const fieldOrInclude = z.union([typeField, includeEntry]);
```

注意：v1 的 superRefine 互斥校验不再需要——只有一个 `type` 键，不存在互斥。Zod `type: z.string().min(1)` 自动拒绝缺 `type` 或空 `type` 的 field。

- [ ] **Step 9: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: 4 条新测试都 PASS。schemas.test.ts 里其他原有 v1 测试此刻会失败（它们用 `base:`/`ref:`）——那些在 Task 11 统一迁移，本任务不管。

**验证范围说明**：本步只看新增的 4 条 v2 测试通过即可，不要被旧测试失败干扰。

- [ ] **Step 10: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 11: 提交**

```bash
git add packages/core/src/ir/schemas.ts packages/core/tests/schemas.test.ts
git commit -m "feat(v2): field schema — single type: key replaces base/ref

Drops baseField/refField union + mutex superRefine in favor of a single
typeField. type: value is a string (single-segment base_types short name
or three-segment value_type ref); form discrimination is deferred to the
typespace resolver (later task).

v1 base/ref fields are now rejected by Zod (no matching union arm).
Four new tests cover: accept single-seg, accept three-seg, reject base,
reject ref."
```

---

## Task 3: 三段名类型引用解析器（ir/typespace.ts 骨架）

**Files:**
- Create: `packages/core/src/ir/typespace.ts`
- Create: `packages/core/tests/typespace.test.ts`
- Modify: `packages/core/src/index.ts`

**背景：** v2 引入独立的类型名字空间（spec §7.2）。类型引用是三段名 `sys.mod.Name`（无 kind），与身份 `kind:sys.mod.Name`（带 kind）分离。这个模块提供类型名字解析的纯函数骨架：

- `parseTypeRef(s: string): { system, module, name } | null` — 单段返回 null（不是类型引用，是 base_types 短名）；三段返回结构化；其他形态返回 null
- 注意：单段 vs 三段的**形态判断**这里做（点号数量），但**名字解析**（查表、消歧）在 Task 5 做（需要 IR 上下文）。这个任务只做纯字符串解析。

- [ ] **Step 1: 写失败测试 — 三段名解析**

创建 `packages/core/tests/typespace.test.ts`：

```typescript
import { describe, expect, it } from 'vitest';
import { parseTypeRef } from '../src/ir/typespace.js';

describe('parseTypeRef', () => {
  it('parses a three-segment type ref', () => {
    expect(parseTypeRef('base.core.Email')).toEqual({
      system: 'base',
      module: 'core',
      name: 'Email',
    });
  });

  it('returns null for a single-segment name (base_types short name)', () => {
    expect(parseTypeRef('integer')).toBeNull();
  });

  it('returns null for a two-segment name (invalid form)', () => {
    expect(parseTypeRef('base.Email')).toBeNull();
  });

  it('returns null for a four-segment name (invalid form)', () => {
    expect(parseTypeRef('a.b.c.d')).toBeNull();
  });

  it('returns null for empty string', () => {
    expect(parseTypeRef('')).toBeNull();
  });

  it('returns null for a name with empty segments', () => {
    expect(parseTypeRef('base..Email')).toBeNull();
    expect(parseTypeRef('.core.Email')).toBeNull();
    expect(parseTypeRef('base.core.')).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- typespace.test.ts`
Expected: FAIL（模块不存在，import 报错）。

- [ ] **Step 3: 创建 typespace.ts**

创建 `packages/core/src/ir/typespace.ts`：

```typescript
/**
 * Type namespace — resolution of `type:` field values. See spec v2 §3, §7.2.
 *
 * v2 splits references into two namespaces:
 *   - identity refs:  `kind:sys.mod.Name`  (four-segment, kind required)
 *                     used by mixin include, primary_table, ref_table, entity
 *   - type refs:      `sys.mod.Name`        (three-segment, no kind)
 *                     used by field `type:`
 *
 * This module handles the *form* of type refs (single-segment vs
 * three-segment). Name *resolution* (looking up the node, checking kind,
 * disambiguating short names via `using`) lives in the loader's link pass
 * and operates on IR context — see Task 5.
 */

/** A parsed three-segment type reference. Single-segment names are NOT type
 *  refs (they are base_types short names); {@link parseTypeRef} returns null
 *  for them. */
export interface TypeRef {
  readonly system: string;
  readonly module: string;
  readonly name: string;
}

/**
 * Parse a `type:` value into a {@link TypeRef}.
 *
 * @returns structured ref for three-segment names; `null` for single-segment
 *          names (base_types short names — not a type ref) or any other form
 *          (the caller treats single-segment as base_types lookup and other
 *          forms as resolution errors).
 */
export function parseTypeRef(s: string): TypeRef | null {
  if (s === '') return null;
  const parts = s.split('.');
  if (parts.length !== 3) return null;
  const [system, module, name] = parts;
  if (system === undefined || module === undefined || name === undefined) return null;
  if (system === '' || module === '' || name === '') return null;
  return { system, module, name };
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- typespace.test.ts`
Expected: 所有 6 条测试 PASS。

- [ ] **Step 5: 导出 typespace**

修改 `packages/core/src/index.ts`，在 `export * from './ir/field.js';` 一行后追加：

```typescript
export * from './ir/typespace.js';
```

- [ ] **Step 6: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 7: 提交**

```bash
git add packages/core/src/ir/typespace.ts packages/core/tests/typespace.test.ts packages/core/src/index.ts
git commit -m "feat(v2): typespace module — type ref form parsing

Adds ir/typespace.ts with parseTypeRef(): a pure-string helper that
classifies a type: value as single-segment (base_types short name → null)
or three-segment (value_type node ref → TypeRef). Form only — actual
name resolution against IR + using imports comes in the next task.

Exported through index.ts. Six tests cover valid three-segment, the four
invalid forms, and empty string."
```

---

## Task 4: `using:` 字段加入 ParsedFileBase + Zod schema

**Files:**
- Modify: `packages/core/src/ir/schemas.ts` (ParsedFileBase + 各 kind schema)
- Test: `packages/core/tests/schemas.test.ts`

**背景：** spec §4.2 规定 using 是文件级。每个 .yaml 文件顶部可以声明 `using:` 列表。base_types.yaml 和 module_manifest.yaml 不需要 using（它们是基础设施），但为了一致性，所有 kind 都接受 using 字段（base_types/module_manifest 通常省略）。default `base.core.*` 是隐含的，**不在文件里写**——由 link 阶段在 Task 5 注入。

using 条目的形态（spec §4.4）：A 与 B 统一为字符串，末尾 `.*` = 模块通配，无 `.*` = 精确名。

```yaml
using:
  - base.core.*           # 模块通配
  - base.core.Email       # 精确名
```

- [ ] **Step 1: 写失败测试 — mixin 接受 using**

在 `packages/core/tests/schemas.test.ts` 的 v2 describe 块追加：

```typescript
  it('accepts an optional using: list on a value_type file', () => {
    const yaml = `version: loom-schema/v2
kind: value_type
name: Order
using:
  - base.core.*
  - retail.pos.types.*
fields:
  - name: value
    type: string
    max_length: 100
`;
    const f = parseFile(yaml, 'test.yaml');
    const data = f.data as { using?: string[] };
    expect(data.using).toEqual(['base.core.*', 'retail.pos.types.*']);
  });

  it('accepts a file with no using: key (default base.core.* is implicit)', () => {
    const yaml = `version: loom-schema/v2
kind: value_type
name: Email
fields:
  - name: value
    type: string
    max_length: 254
`;
    const f = parseFile(yaml, 'test.yaml');
    const data = f.data as { using?: string[] };
    expect(data.using).toBeUndefined();
  });

  it('accepts using: with a single precise name', () => {
    const yaml = `version: loom-schema/v2
kind: mixin
name: M
using:
  - base.core.Email
fields:
  - name: x
    type: Email
`;
    const f = parseFile(yaml, 'test.yaml');
    const data = f.data as { using?: string[] };
    expect(data.using).toEqual(['base.core.Email']);
  });
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: 3 条新测试 FAIL（`data.using` 为 undefined，因为 schema 不认这个键且 strict 模式会拒绝；具体失败信息可能是 Zod 报 unrecognized key）。

- [ ] **Step 3: 改 schemas.ts — 加 using 字段到所有 kind**

修改 `packages/core/src/ir/schemas.ts`。

在文件顶部 import 区下方（versionSchema 定义之前）追加一个共享的 using schema：

```typescript
/**
 * Optional per-file type imports. See spec v2 §4.
 *
 * Each entry is a string:
 *   - `<ns>.*`  → import all types from namespace `<ns>` (module wildcard)
 *   - `<fqtn>`  → import a single type by fully-qualified name
 *
 * Default `base.core.*` is implicit (injected by the link pass) and never
 * written in files.
 */
const usingSchema = z.array(z.string().min(1)).optional();
```

然后在 **每一个** kind schema（BaseTypesSchema / ModuleManifestSchema / ValueTypeSchema / MixinSchema / TableSchema / EntitySchema / ExtensionFieldsSchema）的 `.object({...})` 内部追加 `using: usingSchema,`。

例如 ValueTypeSchema 改成：

```typescript
export const ValueTypeSchema = z
  .object({
    version: versionSchema,
    kind: z.literal('value_type'),
    name: z.string().min(1),
    display_name: z.string().optional(),
    description: z.string().optional(),
    using: usingSchema,
    fields: z.array(fieldOrInclude).min(1),
    constraints: z.array(constraintSchema).optional(),
  })
  .strict();
```

对其他 6 个 kind schema 做相同追加（位置放在 `description` 后面，`fields`/`exports`/`primary_table` 等业务字段前面，保持一致风格）。

注意：每个 schema 都是 `.strict()`，所以必须显式声明 using，否则 Zod 会拒绝。这正是我们要的——强制显式。

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- schemas.test.ts`
Expected: 3 条新 using 测试 PASS。Task 2 的 4 条 type 测试仍然 PASS。

- [ ] **Step 5: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 6: 提交**

```bash
git add packages/core/src/ir/schemas.ts packages/core/tests/schemas.test.ts
git commit -m "feat(v2): per-file using: import list in all kind schemas

Adds an optional 'using' array to every kind schema (base_types,
module_manifest, value_type, mixin, table, entity, extension_fields).
Each entry is a string: <ns>.* for module wildcard or <fqtn> for a
single type. Default base.core.* is implicit and injected at link time,
never written in files.

Three new tests: using on value_type, absent using (undefined), single
precise name on mixin. All kind schemas are .strict() so using must be
declared to be accepted."
```

---

## Task 5: 类型名字空间解析（default `base.core.*` + 文件 using + 短名歧义检测）

**Files:**
- Modify: `packages/core/src/ir/typespace.ts`
- Test: `packages/core/tests/typespace.test.ts`

**背景：** 这是 v2 的核心新逻辑。给定一个文件的 using 列表 + 一个短名 `X`，解析出 `X` 指向哪个全限定类型。规则（spec §4.6）：

1. 单段名先查 base_types 标量注册表（default `base.core.*` 导入）→ 命中即内置标量
2. 单段名再查 using 列表：遍历每条 using，`<ns>.*` 在 `<ns>.X` 查节点；精确名直接匹配
3. 多个 using 命中 → `ambiguous`
4. 都没命中 → `unknown`

这个任务实现**纯函数** `resolveShortName`：输入短名 + using 列表 + 一组"可用全限定名"（候选集），输出解析结果。候选集由调用者（link pass）提供（来自 IR 节点表 + base_types scalars）。

设计为纯函数是为了好测——不需要构造完整 IR。

- [ ] **Step 1: 写失败测试 — 短名在 base_types 命中**

在 `packages/core/tests/typespace.test.ts` 追加：

```typescript
import { resolveShortName, type ResolutionResult } from '../src/ir/typespace.js';

describe('resolveShortName', () => {
  // Candidate set: base_types scalars (single-segment names) + value_type
  // fully-qualified names. Provided by the caller (link pass).
  const scalars = new Set(['integer', 'string', 'decimal', 'datetime', 'boolean', 'enum']);
  const valueTypes = new Set([
    'base.core.Email',
    'base.core.Money',
    'retail.pos.types.Money',
  ]);

  it('resolves a base_types scalar short name regardless of using', () => {
    const result = resolveShortName('integer', [], scalars, valueTypes);
    expect(result.kind).toBe('scalar');
    if (result.kind === 'scalar') expect(result.name).toBe('integer');
  });

  it('resolves a value_type short name via module wildcard using', () => {
    const result = resolveShortName('Email', ['base.core.*'], scalars, valueTypes);
    expect(result.kind).toBe('value_type');
    if (result.kind === 'value_type') expect(result.fqn).toBe('base.core.Email');
  });

  it('resolves a value_type short name via precise using', () => {
    const result = resolveShortName('Email', ['base.core.Email'], scalars, valueTypes);
    expect(result.kind).toBe('value_type');
    if (result.kind === 'value_type') expect(result.fqn).toBe('base.core.Email');
  });

  it('reports ambiguous when two wildcards both match', () => {
    const result = resolveShortName('Money', ['base.core.*', 'retail.pos.types.*'], scalars, valueTypes);
    expect(result.kind).toBe('ambiguous');
    if (result.kind === 'ambiguous') {
      expect(result.candidates).toEqual(
        expect.arrayContaining(['base.core.Money', 'retail.pos.types.Money']),
      );
    }
  });

  it('reports unknown when nothing matches', () => {
    const result = resolveShortName('Nonexistent', ['base.core.*'], scalars, valueTypes);
    expect(result.kind).toBe('unknown');
  });

  it('precise using wins over wildcard (no ambiguity)', () => {
    // Even though base.core.* would also match, an explicit precise import
    // pins the name unambiguously.
    const result = resolveShortName(
      'Money',
      ['base.core.*', 'base.core.Money'],
      scalars,
      valueTypes,
    );
    expect(result.kind).toBe('value_type');
    if (result.kind === 'value_type') expect(result.fqn).toBe('base.core.Money');
  });

  it('scalar lookup takes precedence over value_type using', () => {
    // If a short name collides between a scalar and a value_type via using,
    // the scalar (default namespace) wins — that matches "base_types always
    // available" semantics.
    const scalarsWithConflict = new Set(['Money']);
    const result = resolveShortName('Money', ['base.core.*'], scalarsWithConflict, valueTypes);
    expect(result.kind).toBe('scalar');
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- typespace.test.ts`
Expected: 新增 7 条测试 FAIL（`resolveShortName` 不存在）。

- [ ] **Step 3: 实现 resolveShortName**

修改 `packages/core/src/ir/typespace.ts`，在文件末尾追加：

```typescript
/**
 * Result of resolving a short name against a candidate type namespace.
 *
 *   scalar     — matched a base_types scalar (default namespace, always wins)
 *   value_type — matched a value_type node via using imports
 *   ambiguous  — multiple using imports matched; caller must emit a diagnostic
 *   unknown    — nothing matched; caller must emit a diagnostic
 */
export type ResolutionResult =
  | { readonly kind: 'scalar'; readonly name: string }
  | { readonly kind: 'value_type'; readonly fqn: string }
  | { readonly kind: 'ambiguous'; readonly candidates: readonly string[] }
  | { readonly kind: 'unknown' };

/**
 * Resolve a single-segment short name against the type namespace.
 *
 * @param shortName  the bare name (e.g. "integer", "Email")
 * @param using      the file's explicit using list (wildcards `ns.*` and
 *                   precise names `ns.Name`); default `base.core.*` is NOT
 *                   included here — the caller adds it for scalars only
 *                   (scalars are always available, value_types are not)
 * @param scalars    base_types scalar short-name set (the implicit namespace)
 * @param valueTypes fully-qualified value_type names available in the IR
 *
 * Resolution order (spec §4.6):
 *   1. scalar lookup (default namespace, always wins)
 *   2. precise using entries (direct fqn match)
 *   3. wildcard using entries (ns.* → ns.shortName if it exists in valueTypes)
 *   4. ambiguity if >1 distinct fqn across steps 2-3
 *   5. unknown
 */
export function resolveShortName(
  shortName: string,
  using: readonly string[],
  scalars: ReadonlySet<string>,
  valueTypes: ReadonlySet<string>,
): ResolutionResult {
  // Step 1: scalar default namespace.
  if (scalars.has(shortName)) {
    return { kind: 'scalar', name: shortName };
  }

  // Step 2-3: collect candidate fqns from using entries.
  const candidates = new Set<string>();
  for (const entry of using) {
    if (entry.endsWith('.*')) {
      const ns = entry.slice(0, -2);
      const fqn = `${ns}.${shortName}`;
      if (valueTypes.has(fqn)) candidates.add(fqn);
    } else {
      // Precise name: matches only if its last segment equals shortName.
      const lastDot = entry.lastIndexOf('.');
      const lastName = lastDot < 0 ? entry : entry.slice(lastDot + 1);
      if (lastName === shortName && valueTypes.has(entry)) {
        candidates.add(entry);
      }
    }
  }

  if (candidates.size === 0) return { kind: 'unknown' };
  if (candidates.size === 1) {
    const fqn = [...candidates][0] as string;
    return { kind: 'value_type', fqn };
  }
  return { kind: 'ambiguous', candidates: [...candidates].sort() };
}
```

注意歧义时排序（`[...candidates].sort()`），保证错误信息确定性（spec §10 待定项之一，这里先定下：按字典序排序，稳定）。

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- typespace.test.ts`
Expected: 所有测试 PASS（前 6 条 parseTypeRef + 7 条 resolveShortName）。

- [ ] **Step 5: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 6: 提交**

```bash
git add packages/core/src/ir/typespace.ts packages/core/tests/typespace.test.ts
git commit -m "feat(v2): resolveShortName — type namespace resolution

Core new logic. Given a single-segment short name + a file's using list
+ the candidate sets (base_types scalars + value_type fqns), resolves to
one of: scalar (default namespace wins), value_type (matched via using),
ambiguous (>1 candidate), unknown (no match).

Pure function for testability — the link pass wires real IR data in.
Resolution order matches spec §4.6: scalar → precise using → wildcard
using → ambiguity (sorted for deterministic diagnostics). Seven tests
cover each branch plus the precedence rules."
```

---

## Task 6: link 阶段集成 typespace（替换 v1 的 ref 解析）

**Files:**
- Modify: `packages/core/src/loader/link.ts`
- Test: `packages/core/tests/link.test.ts`

**背景：** v1 link.ts 的 `resolveFieldRefs` 遍历 fields，对 `'ref' in e` 的字段调用 `parseRef`（带 kind 前缀的身份 ref），验证 kind === 'value_type'。v2 字段不再有 `ref`，而是 `type`。新逻辑：

1. 收集全 IR 的 value_type fqn 集合（`sys.mod.Name`）
2. 收集 base_types scalars 集合
3. 对每个有 fields 的文件，对每个字段的 `type:` 值：
   - 单段名 → `resolveShortName(name, fileUsing, scalars, valueTypes)` → 按 result 处理（scalar/value_type/ambiguous/unknown）
   - 三段名 → `parseTypeRef` → 直接是 value_type fqn，验证节点存在且 kind === 'value_type'
4. 把解析结果回填到字段（添加一个内部字段 `_resolved_type`，供后续 validate/projector 用）；或返回新的字段结构

**设计决策**：不修改字段对象的运行时形状（保持 Zod 解析结果不变），而是在 link pass 里构造一个**旁路映射**：`Map<fieldIdentity, ResolvedType>`。但这会复杂化下游。更简单的方案：link pass 直接把 `type:` 值**重写**为已解析的 fqn（单段名保持单段名=base_types；三段名保持三段名=value_type fqn）。这样：

- 单段名 `type: integer` 保持不变（base_types 短名）
- 短名 via using `type: Email` 重写为 `type: base.core.Email`
- 三段名 `type: base.core.Email` 保持不变

下游 validate/projector 只需区分"单段 vs 三段"，不再需要 using 上下文。

- [ ] **Step 1: 看 link.test.ts 现有结构**

Run: `cat packages/core/tests/link.test.ts`
记录现有测试用了什么固件、断言什么。

- [ ] **Step 2: 写失败测试 — v2 link 解析短名 value_type**

在 `packages/core/tests/link.test.ts` 顶部 import 后追加（如果还没有 buildBaseSchemaFs import，加上）：

```typescript
import { link } from '../src/loader/link.js';
import { parseAll } from '../src/loader/parse.js';
import { discover } from '../src/loader/discovery.js';
import { Diagnostics } from '../src/errors.js';
import { MemoryFileSystem } from './fixtures/memory_fs.js';

async function linkFromStringMap(files: Record<string, string>) {
  const fs = new MemoryFileSystem(files);
  const diag = new Diagnostics();
  const { files: discovered } = await discover({ fs, basePath: '', diagnostics: diag });
  const { parsed } = await parseAll({ fs, files: discovered, diagnostics: diag });
  return link({ parsed, diagnostics: diag });
}

describe('v2 link — type resolution', () => {
  it('resolves a value_type short name via using wildcard', async () => {
    const { ir, diagnostics } = await linkFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: bigint, description: i, properties: [] }
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/email.yaml': `version: loom-schema/v2
kind: value_type
name: Email
fields:
  - { name: value, type: string }
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users_base
  extension: { strategy: none }
fields:
  - { name: id, type: bigint, required: true }
  - { name: email, type: Email, required: true, unique: true }
primary_key: [id]
`,
    });

    expect(diagnostics.hasErrors).toBe(false);
    const usersNode = [...ir.nodes.values()].find((n) => n.kind === 'table');
    expect(usersNode).toBeDefined();
    const fields = (usersNode?.data as { fields: Array<Record<string, unknown>> }).fields;
    // Short name "Email" should be rewritten to its fqn after link.
    const emailField = fields.find((f) => f.name === 'email');
    expect(emailField?.type).toBe('base.core.Email');
  });

  it('reports ambiguous when using imports two modules with the same type name', async () => {
    const { diagnostics } = await linkFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/money.yaml': `version: loom-schema/v2
kind: value_type
name: Money
fields:
  - { name: amount, type: string }
`,
      'systems/retail/pos/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: retail
module: pos
physical_schema: retail_pos
`,
      'systems/retail/pos/value_type/money.yaml': `version: loom-schema/v2
kind: value_type
name: Money
fields:
  - name: value
    type: string
using:
  - base.core.*
  - retail.pos.types.*
`,
      'systems/retail/pos/table/orders.yaml': `version: loom-schema/v2
kind: table
name: Orders
using:
  - base.core.*
  - retail.pos.types.*
table:
  name: orders
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: total, type: Money }
primary_key: [id]
`,
    });

    expect(diagnostics.hasErrors).toBe(true);
    const ambiguousMsgs = diagnostics.all.filter(
      (d) => d.message.includes('ambiguous'),
    );
    expect(ambiguousMsgs.length).toBeGreaterThan(0);
  });

  it('reports unknown type when short name matches nothing', async () => {
    const { diagnostics } = await linkFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: x, type: Nonexistent }
primary_key: [id]
`,
    });

    expect(diagnostics.hasErrors).toBe(true);
    expect(diagnostics.all.some((d) => d.message.includes('unknown type'))).toBe(true);
  });

  it('reports kind_mismatch when a three-segment type ref targets a non-value_type', async () => {
    const { diagnostics } = await linkFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: x, type: base.core.Users }
primary_key: [id]
`,
    });

    expect(diagnostics.hasErrors).toBe(true);
    expect(diagnostics.all.some((d) => d.category === 'kind_mismatch')).toBe(true);
  });
});
```

- [ ] **Step 3: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- link.test.ts`
Expected: 4 条新测试 FAIL（link 还在用 v1 的 ref 解析逻辑）。

- [ ] **Step 4: 重写 link.ts 的字段解析部分**

修改 `packages/core/src/loader/link.ts`。

**4a. 改 import 区**，把：

```typescript
import type { Diagnostics } from '../errors.js';
import { parseRef } from '../ir/refs.js';
import type { AnyFile } from '../ir/schemas.js';
import type { IR, IRNode, Identity } from '../ir/version.js';
import type { FileKind } from '../ir/version.js';
import { CURRENT_VERSION } from '../ir/version.js';
```

改为：

```typescript
import type { Diagnostics } from '../errors.js';
import { parseRef } from '../ir/refs.js';
import { parseTypeRef, resolveShortName, type TypeRef } from '../ir/typespace.js';
import type { AnyFile, BaseTypes } from '../ir/schemas.js';
import type { IR, IRNode, Identity } from '../ir/version.js';
import { CURRENT_VERSION } from '../ir/version.js';
```

（移除未使用的 `FileKind` import；加 typespace + BaseTypes）

**4b. 删除 v1 的 FIELD_REF_KINDS**：

删除：
```typescript
/** Ref kinds legal in a `ref:` field of a fields[] entry. */
const FIELD_REF_KINDS: ReadonlySet<FileKind> = new Set<FileKind>(['value_type']);
```

INCLUDE_REF_KINDS 保留（mixin include 语法不变，spec §5）。

**4c. 改 link() 主体**——在循环外先收集 base_types scalars 和 value_type fqns：

把当前 `link()` 函数体（从 `export async function link` 到 `return { ir, diagnostics: opts.diagnostics };`）替换为：

```typescript
export async function link(opts: LinkOptions): Promise<LinkResult> {
  const nodes = new Map<Identity, IRNode>();
  const deps = new Map<Identity, Set<Identity>>();

  // Lift all parsed files into IRNodes keyed by identity.
  for (const [identity, file] of opts.parsed) {
    nodes.set(identity, { ...file, identity });
    deps.set(identity, new Set());
  }

  // Collect candidate sets for type resolution.
  const scalars = collectScalars(opts.parsed);
  const valueTypes = collectValueTypeFqns(opts.parsed);

  // Resolve type refs + expand mixins per file.
  for (const [identity, node] of nodes) {
    const fieldsHost = fieldsOf(node);
    if (fieldsHost === null) continue;

    const expanded = expandIncludes(fieldsHost, identity, opts.parsed, opts.diagnostics, deps);
    if (expanded === null) continue;

    const fileUsing = collectUsing(node);
    resolveFieldTypes(
      expanded,
      identity,
      fileUsing,
      scalars,
      valueTypes,
      opts.parsed,
      opts.diagnostics,
      deps,
    );

    nodes.set(identity, withFields(node, expanded));
  }

  const ir: IR = {
    nodes: nodes as ReadonlyMap<Identity, IRNode>,
    deps: deps as ReadonlyMap<Identity, ReadonlySet<Identity>>,
    version: CURRENT_VERSION,
  };
  return { ir, diagnostics: opts.diagnostics };
}
```

**4d. 在文件 helper 区追加新函数**（在 `function fieldsOf` 之前或之后都行，保持就近原则）：

```typescript
/** Collect base_types scalar short names from the parsed set. */
function collectScalars(parsed: ReadonlyMap<string, AnyFile>): Set<string> {
  const scalars = new Set<string>();
  for (const f of parsed.values()) {
    if (f.kind !== 'base_types') continue;
    const data = f.data as BaseTypes;
    for (const s of data.scalars) scalars.add(s.name);
  }
  return scalars;
}

/** Collect value_type fully-qualified names (sys.mod.Name) from the parsed set. */
function collectValueTypeFqns(parsed: ReadonlyMap<string, AnyFile>): Set<string> {
  const fqns = new Set<string>();
  for (const [identity, f] of parsed) {
    if (f.kind !== 'value_type') continue;
    // Identity format: value_type:sys.mod.Name
    const colonIdx = identity.indexOf(':');
    if (colonIdx < 0) continue;
    fqns.add(identity.slice(colonIdx + 1));
  }
  return fqns;
}

/** Read the file's optional using list (empty if absent). */
function collectUsing(node: IRNode): readonly string[] {
  const data = node.data as { using?: unknown };
  if (Array.isArray(data.using)) return data.using.filter((s): s is string => typeof s === 'string');
  return [];
}
```

**4e. 替换 resolveFieldRefs 为 resolveFieldTypes**：

删除整个 `function resolveFieldRefs(...)` 函数。替换为：

```typescript
/**
 * Resolve every field's `type:` value (spec v2 §3, §4.6).
 *
 *   - single-segment name  → resolveShortName against scalars + using
 *   - three-segment name   → direct fqn; verify target is a value_type node
 *
 * Single-segment names that resolve to scalars stay as-is (base_types short
 * names flow through to the projector). Names resolved via using are
 * rewritten to their fqn so downstream passes don't need the using context.
 */
function resolveFieldTypes(
  host: FieldsHost,
  identity: Identity,
  using: readonly string[],
  scalars: ReadonlySet<string>,
  valueTypes: ReadonlySet<string>,
  parsed: ReadonlyMap<string, AnyFile>,
  diag: Diagnostics,
  deps: Map<Identity, Set<Identity>>,
): void {
  for (const e of host.fields) {
    if (typeof e !== 'object' || e === null) continue;
    if (!('type' in e)) continue;
    const typeVal = (e as { type: unknown }).type;
    if (typeof typeVal !== 'string') continue;

    const threeSeg = parseTypeRef(typeVal);
    if (threeSeg !== null) {
      // Three-segment: direct value_type ref.
      resolveThreeSegment(e as Record<string, unknown>, threeSeg, identity, parsed, diag, deps);
    } else {
      // Single-segment (or invalid form): treat as short name.
      resolveSingleSegment(
        e as Record<string, unknown>,
        typeVal,
        identity,
        using,
        scalars,
        valueTypes,
        parsed,
        diag,
        deps,
      );
    }
  }
}

function resolveThreeSegment(
  field: Record<string, unknown>,
  ref: TypeRef,
  identity: Identity,
  parsed: ReadonlyMap<string, AnyFile>,
  diag: Diagnostics,
  deps: Map<Identity, Set<Identity>>,
): void {
  const targetId = `value_type:${ref.system}.${ref.module}.${ref.name}`;
  const target = parsed.get(targetId);
  if (!target) {
    diag.add({
      category: 'dangling_ref',
      file: identity,
      line: 1,
      column: 1,
      message: `unknown type "${targetId.slice('value_type:'.length)}"`,
    });
    return;
  }
  if (target.kind !== 'value_type') {
    diag.add({
      category: 'kind_mismatch',
      file: identity,
      line: 1,
      column: 1,
      message: `type reference "${targetId.slice('value_type:'.length)}" resolves to kind=${target.kind}, expected value_type`,
    });
    return;
  }
  deps.get(identity)?.add(targetId);
}

function resolveSingleSegment(
  field: Record<string, unknown>,
  shortName: string,
  identity: Identity,
  using: readonly string[],
  scalars: ReadonlySet<string>,
  valueTypes: ReadonlySet<string>,
  parsed: ReadonlyMap<string, AnyFile>,
  diag: Diagnostics,
  deps: Map<Identity, Set<Identity>>,
): void {
  // Form validation: single-segment means "no dots". If the caller somehow
  // passed something with dots, that's an invalid form caught elsewhere.
  if (shortName.includes('.')) {
    diag.add({
      category: 'parse',
      file: identity,
      line: 1,
      column: 1,
      message: `invalid type reference "${shortName}"`,
    });
    return;
  }

  const result = resolveShortName(shortName, using, scalars, valueTypes);
  switch (result.kind) {
    case 'scalar':
      // Stays as-is; base_types short name flows to the projector.
      return;
    case 'value_type': {
      // Rewrite the field's type to its fqn.
      field.type = result.fqn;
      const targetId = `value_type:${result.fqn}`;
      deps.get(identity)?.add(targetId);
      return;
    }
    case 'ambiguous':
      diag.add({
        category: 'schema',
        file: identity,
        line: 1,
        column: 1,
        message: `ambiguous type reference "${shortName}", candidates: ${result.candidates.join(', ')}`,
      });
      return;
    case 'unknown':
      diag.add({
        category: 'schema',
        file: identity,
        line: 1,
        column: 1,
        message: `unknown type "${shortName}"`,
      });
      return;
  }
}
```

**4f. 删除现在不用的 safeParseRef / refToIdentity**：

检查 `safeParseRef` 和 `refToIdentity` 是否还被 `expandIncludes` 用——是的，`expandIncludes` 还在用它们解析 mixin include（mixin 仍走身份 ref）。**保留这两个函数**。

- [ ] **Step 5: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- link.test.ts`
Expected: 4 条新测试 PASS。link.test.ts 里的旧 v1 测试此刻会失败——那些在 Task 11 迁移，本任务不管。

- [ ] **Step 6: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。如果有 unused import（比如 FileKind），按提示删除。

- [ ] **Step 7: 提交**

```bash
git add packages/core/src/loader/link.ts packages/core/tests/link.test.ts
git commit -m "feat(v2): link pass — type resolution via typespace

Replaces v1 resolveFieldRefs (which parsed kind:sys.mod.Name identity
refs on field 'ref:') with resolveFieldTypes, which handles v2's single
type: key:

- three-segment names verified directly against value_type nodes
- single-segment names resolved via resolveShortName (scalars default
  namespace + file's using list); value_type matches rewrite the field's
  type to its fqn so downstream passes don't need using context

Collects scalars + value_type fqn sets once per pass. Four new tests:
short-name via wildcard, ambiguity across two wildcards, unknown type,
kind_mismatch when a three-segment ref targets a non-value_type.

mixin include resolution is unchanged — still uses kind:sys.mod.Name
identity refs (spec §5 defers mixin using-ification to v2.1)."
```

---

## Task 7: validate 阶段重写（checkTypedField）

**Files:**
- Modify: `packages/core/src/loader/validate.ts`
- Test: `packages/core/tests/validate.test.ts`

**背景：** v1 validate.ts 的 `checkScalarField` 检查字段的 `base` 是否在 base_types scalars 里、必填属性是否齐全。v2 字段不再有 `base`，而是 `type`。经过 link pass，单段名只可能是 base_types scalar（其他都被改写或报错了），三段名只可能是 value_type fqn。

新逻辑 `checkTypedField`：

- 如果 `type` 是单段名（无点）→ 当作 scalar，检查在 base_types 里 + 必填属性
- 如果 `type` 是三段名 → 跳过 scalar 检查（value_type 的内部字段已经在 value_type 文件里被检查过）
- primary_key 检查不变

- [ ] **Step 1: 看 validate.test.ts 现有测试**

Run: `cat packages/core/tests/validate.test.ts`
记录测试结构。

- [ ] **Step 2: 写失败测试 — v2 unknown scalar 检测**

在 `packages/core/tests/validate.test.ts` 顶部追加 helper + 测试：

```typescript
import { validate } from '../src/loader/validate.js';
import { link } from '../src/loader/link.js';
import { parseAll } from '../src/loader/parse.js';
import { discover } from '../src/loader/discovery.js';
import { Diagnostics } from '../src/errors.js';
import { MemoryFileSystem } from './fixtures/memory_fs.js';

async function validateFromStringMap(files: Record<string, string>) {
  const fs = new MemoryFileSystem(files);
  const diag = new Diagnostics();
  const { files: discovered } = await discover({ fs, basePath: '', diagnostics: diag });
  const { parsed } = await parseAll({ fs, files: discovered, diagnostics: diag });
  const { ir } = await link({ parsed, diagnostics: diag });
  validate({ ir, diagnostics: diag });
  return diag;
}

describe('v2 validate — typed fields', () => {
  it('reports unknown scalar for a single-segment type not in base_types', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: age, type: notAScalar }
primary_key: [id]
`,
    });

    expect(diag.hasErrors).toBe(true);
    expect(diag.all.some((d) => d.message.includes('unknown scalar') && d.message.includes('notAScalar'))).toBe(true);
  });

  it('reports missing required scalar property (decimal needs precision/scale)', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - name: decimal
    description: d
    properties:
      - { name: precision, type: integer, required: true }
      - { name: scale, type: integer, required: true }
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/money.yaml': `version: loom-schema/v2
kind: value_type
name: Money
fields:
  - name: amount
    type: decimal
    precision: 18
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: balance, type: Money }
primary_key: [id]
`,
    });

    expect(diag.hasErrors).toBe(true);
    expect(diag.all.some((d) => d.message.includes('scale'))).toBe(true);
  });

  it('passes when a three-segment value_type ref is used (no scalar check on the ref)', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/email.yaml': `version: loom-schema/v2
kind: value_type
name: Email
fields:
  - { name: value, type: string }
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: email, type: base.core.Email }
primary_key: [id]
`,
    });

    expect(diag.hasErrors).toBe(false);
  });

  it('still checks primary_key fields are required:true', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string }
primary_key: [id]
`,
    });

    expect(diag.hasErrors).toBe(true);
    expect(diag.all.some((d) => d.message.includes('primary_key') && d.message.includes('required'))).toBe(true);
  });
});
```

- [ ] **Step 3: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- validate.test.ts`
Expected: 新测试 FAIL（validate 还在用 `base` 字段，找不到）。

- [ ] **Step 4: 改 validate.ts**

修改 `packages/core/src/loader/validate.ts`。

**4a. 把 `checkScalarField` 重命名 + 改签名 + 改逻辑**。把整个 `function checkScalarField(...)` 替换为：

```typescript
/**
 * Check a field's `type:` value (spec v2).
 *
 *   - single-segment name  → must be a base_types scalar; required
 *                             properties must be present
 *   - three-segment name   → skip (value_type's own fields are validated
 *                             when the value_type file is visited)
 *
 * Note: by the time validate runs, the link pass has already rewritten
 * short-name value_type matches to their fqn. So any single-segment name
 * still present here is intended as a scalar.
 */
function checkTypedField(
  identity: string,
  f: FieldLike,
  scalarNames: Set<string>,
  requiredProps: Map<string, Set<string>>,
  diag: Diagnostics,
): void {
  if (scalarNames.size === 0) return;
  const typeVal = f.type;
  if (typeof typeVal !== 'string') return;
  // Three-segment (value_type ref): skip — validated at the value_type file.
  if (typeVal.includes('.')) return;

  // Single-segment: must be a known scalar.
  if (!scalarNames.has(typeVal)) {
    diag.add({
      category: 'schema',
      file: identity,
      line: 1,
      column: 1,
      message: `unknown scalar type "${typeVal}" (not in base_types)`,
    });
    return;
  }
  const req = requiredProps.get(typeVal) ?? new Set<string>();
  for (const rp of req) {
    if (!(rp in f)) {
      diag.add({
        category: 'schema',
        file: identity,
        line: 1,
        column: 1,
        message: `scalar "${typeVal}" requires property "${rp}" (base_types)`,
      });
    }
  }
}
```

**4b. 改 validate() 主体里所有 `checkScalarField` 调用为 `checkTypedField`**。

在 `validate()` 函数内搜索 `checkScalarField` 调用点（应该有两处：value_type/mixin/extension_fields 分支里，table 分支里通过 checkTable）。把所有调用改为 `checkTypedField`。

具体改：

在 `case 'value_type': case 'mixin': case 'extension_fields':` 分支里：
```typescript
checkScalarField(identity, f, scalarNames, requiredProps, opts.diagnostics);
```
改为：
```typescript
checkTypedField(identity, f, scalarNames, requiredProps, opts.diagnostics);
```

在 `checkTable()` 函数里：
```typescript
checkScalarField(identity, fieldRec, scalarNames, requiredProps, diag);
```
改为：
```typescript
checkTypedField(identity, fieldRec, scalarNames, requiredProps, diag);
```

**4c. 改文件顶部注释**。把：

```typescript
/**
 * Pass 3 — semantic validation. See spec §13.1, §10, §6.9, §7.5.
 *
 * Cross-file rules that Zod cannot express:
 *   - every field `base` is a scalar declared in base_types
 *   - every scalar property flagged required in base_types is present on the field
 *   - table.primary_key entries are all required:true fields
 *   - extension_fields targets an entity whose primary_table is sidecar_eav
 */
```

改为：

```typescript
/**
 * Pass 3 — semantic validation. See spec §13.1, §6.9, §7.5 (v2).
 *
 * Cross-file rules that Zod cannot express:
 *   - every field `type:` single-segment name is a scalar declared in base_types
 *   - every scalar property flagged required in base_types is present on the field
 *   - table.primary_key entries are all required:true fields
 *   - extension_fields targets an entity whose primary_table is sidecar_eav
 *
 * (v2: three-segment value_type refs are validated at the value_type file.)
 */
```

- [ ] **Step 5: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- validate.test.ts`
Expected: 4 条新测试 PASS。

- [ ] **Step 6: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 7: 提交**

```bash
git add packages/core/src/loader/validate.ts packages/core/tests/validate.test.ts
git commit -m "feat(v2): validate pass — checkTypedField replaces checkScalarField

Fields no longer carry 'base'; they carry 'type'. After link rewriting,
single-segment names that survive are intended scalars (short-name
value_type matches were rewritten to fqns). So checkTypedField:

- single-segment → verify scalar in base_types + required props present
- three-segment → skip (validated at the value_type file)

primary_key required:true check unchanged. Four new tests: unknown
scalar, missing required prop (decimal scale), valid three-segment ref,
primary_key-not-required."
```

---

## Task 8: enum 强制走 value_type（移除 inline enum 校验路径）

**Files:**
- Modify: `packages/core/src/loader/validate.ts` (确认 enum 仍走 value_type 路径)
- Modify: `packages/core/src/projector/expand.ts` (enumRef 来源从 base===enum 改为 type===enum)
- Test: `packages/core/tests/validate.test.ts`

**背景：** spec §6 决策——enum 强制走 value_type，取消 inline enum。v1 schemas.ts 里 inline enum 是通过 `base: enum` + `values: [...]` 表达的，被 `baseField.catchall` 允许。v2 字段是 `type:`，inline enum 会写成 `type: enum` + `values: [...]`。

要"取消 inline enum"有两种实现：
1. 在 Zod schema 里拒绝 `type: enum`（但这会误伤 value_type 内部的 `type: enum`）
2. 在 validate 里检测：如果字段 `type: enum` 且有 `values` 且**不在** value_type 文件内 → 报错

方案 2 更精确（enum 标量本身是合法的，它出现在 value_type 文件内部是允许的；出现在 table/mixin/extension_fields 里是 inline enum，禁止）。

- [ ] **Step 1: 写失败测试 — table 里 inline enum 被拒**

在 `packages/core/tests/validate.test.ts` 的 v2 describe 块追加：

```typescript
  it('rejects inline enum in a table field (enum must go through value_type)', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - name: enum
    description: e
    properties:
      - { name: values, type: string, required: true }
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - name: status
    type: enum
    values: [active, inactive]
primary_key: [id]
`,
    });

    expect(diag.hasErrors).toBe(true);
    expect(diag.all.some((d) => d.message.includes('inline enum'))).toBe(true);
  });

  it('allows type: enum inside a value_type file (enum definition)', async () => {
    const diag = await validateFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - name: enum
    description: e
    properties:
      - { name: values, type: string, required: true }
  - { name: string, description: s, properties: [] }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/status.yaml': `version: loom-schema/v2
kind: value_type
name: Status
fields:
  - name: value
    type: enum
    values: [active, inactive]
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: string, required: true }
  - { name: status, type: Status }
primary_key: [id]
`,
    });

    // The inline-enum check should NOT fire for the value_type file.
    const inlineEnumErrors = diag.all.filter((d) => d.message.includes('inline enum'));
    expect(inlineEnumErrors).toEqual([]);
  });
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- validate.test.ts`
Expected: 第一条新测试 FAIL（inline enum 没被检测到，因为 validate 还没这条规则）；第二条可能 PASS（因为没有规则就不会报错）。

- [ ] **Step 3: 在 validate.ts 加 inline enum 检测**

修改 `packages/core/src/loader/validate.ts` 的 `validate()` 主体。在 `switch (node.kind)` 的 `case 'value_type'` 之前，给 `case 'mixin'`、`case 'extension_fields'`、以及 `case 'table'` 的字段循环里加 inline enum 检测。

最简单的方式：在 `checkTypedField` 内部，当 typeVal === 'enum' 时，如果字段有 `values` 属性，就报 inline enum。但这会在 value_type 文件里也报错。所以需要把"是否在 value_type 文件内"作为参数传入。

**实现**：给 `checkTypedField` 加一个 `hostKind` 参数：

修改 `checkTypedField` 签名，在 `scalarNames` 参数前加 `hostKind: FileKind`：

```typescript
function checkTypedField(
  identity: string,
  hostKind: FileKind,
  f: FieldLike,
  scalarNames: Set<string>,
  requiredProps: Map<string, Set<string>>,
  diag: Diagnostics,
): void {
  if (scalarNames.size === 0) return;
  const typeVal = f.type;
  if (typeof typeVal !== 'string') return;

  // Inline enum is only legal inside a value_type file (spec v2 §6).
  if (typeVal === 'enum' && hostKind !== 'value_type') {
    diag.add({
      category: 'schema',
      file: identity,
      line: 1,
      column: 1,
      message: `inline enum is not allowed in ${hostKind} (define a value_type and reference it)`,
    });
    return;
  }

  // Three-segment (value_type ref): skip.
  if (typeVal.includes('.')) return;

  if (!scalarNames.has(typeVal)) {
    // ... (rest unchanged)
```

**更新调用点**：在 `validate()` 的 value_type/mixin/extension_fields 分支：

```typescript
case 'value_type':
case 'mixin':
case 'extension_fields': {
  const data = node.data as { fields?: FieldLike[] };
  for (const f of data.fields ?? []) {
    checkTypedField(identity, node.kind, f, scalarNames, requiredProps, opts.diagnostics);
  }
  ...
```

在 `checkTable()` 内部（需要把 hostKind 传进去，或者直接硬编码 'table'）：

修改 `checkTable` 签名加 hostKind，或者直接在调用 checkTypedField 时传 `'table'`：

```typescript
function checkTable(
  identity: string,
  t: Table,
  scalarNames: Set<string>,
  requiredProps: Map<string, Set<string>>,
  diag: Diagnostics,
): void {
  const requiredFieldNames = new Set<string>();
  for (const f of t.fields) {
    const fieldRec = f as FieldLike;
    if (typeof fieldRec.name === 'string' && fieldRec.required === true) {
      requiredFieldNames.add(fieldRec.name);
    }
    checkTypedField(identity, 'table', fieldRec, scalarNames, requiredProps, diag);
  }
  // ... rest unchanged
```

记得 import `FileKind` 类型（在 validate.ts 顶部 import 区加 `import type { FileKind } from '../ir/version.js';`）。

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- validate.test.ts`
Expected: 两条新测试 PASS（inline enum 在 table 被拒；在 value_type 被允许）。

- [ ] **Step 5: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 6: 提交**

```bash
git add packages/core/src/loader/validate.ts packages/core/tests/validate.test.ts
git commit -m "feat(v2): ban inline enum outside value_type files (spec §6)

enum is a legal base_types scalar, but its values list may only be
declared inside a value_type file. checkTypedField now takes the host
kind and rejects type: enum + values on mixin/table/extension_fields
hosts with a clear message pointing at value_type.

value_type files retain type: enum (that's how enums are defined). Two
new tests: inline enum in table rejected, type: enum inside value_type
allowed."
```

---

## Task 9: projector expand.ts 改造（单一 type 分支）

**Files:**
- Modify: `packages/core/src/projector/expand.ts:206-280` (expandField)
- Test: `packages/core/tests/projector-expand.test.ts`

**背景：** v1 expandField 区分 `'base' in f`（直接标量）和 `'ref' in f`（value_type 引用）。v2 字段只有 `type`。经过 link pass：

- 单段名 `type: integer` → 直接标量
- 三段名 `type: base.core.Email`（或经 using 重写的 fqn）→ value_type 引用

所以 expandField 只需看 `type` 是否包含点号。

- [ ] **Step 1: 看 projector-expand.test.ts 现有测试结构**

Run: `cat packages/core/tests/projector-expand.test.ts`
记录用了什么固件、断言什么。

- [ ] **Step 2: 写失败测试 — v2 type: 单段名展开为 1 列**

在 `packages/core/tests/projector-expand.test.ts` 顶部追加：

```typescript
import { expandTables } from '../src/projector/expand.js';
import { link } from '../src/loader/link.js';
import { parseAll } from '../src/loader/parse.js';
import { discover } from '../src/loader/discovery.js';
import { Diagnostics } from '../src/errors.js';
import { MemoryFileSystem } from './fixtures/memory_fs.js';

async function expandFromStringMap(files: Record<string, string>) {
  const fs = new MemoryFileSystem(files);
  const diag = new Diagnostics();
  const { files: discovered } = await discover({ fs, basePath: '', diagnostics: diag });
  const { parsed } = await parseAll({ fs, files: discovered, diagnostics: diag });
  const { ir } = await link({ parsed, diagnostics: diag });
  return expandTables(ir);
}

describe('v2 projector — expandField via type:', () => {
  it('expands a single-segment type to one column', async () => {
    const model = await expandFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: bigint, description: i, properties: [] }
  - name: string
    description: s
    properties:
      - { name: max_length, type: integer, required: true }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: bigint, required: true }
  - name: email
    type: string
    max_length: 254
primary_key: [id]
`,
    });

    const users = model.tables.find((t) => t.name === 'users');
    expect(users).toBeDefined();
    const cols = users?.columns.map((c) => `${c.name}:${c.scalar}`) ?? [];
    expect(cols).toContain('email:string');
  });

  it('expands a three-segment single-field value_type ref to one column (no suffix)', async () => {
    const model = await expandFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: bigint, description: i, properties: [] }
  - name: string
    description: s
    properties:
      - { name: max_length, type: integer, required: true }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/email.yaml': `version: loom-schema/v2
kind: value_type
name: Email
fields:
  - name: value
    type: string
    max_length: 254
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: bigint, required: true }
  - { name: email, type: Email, required: true }
primary_key: [id]
`,
    });

    const users = model.tables.find((t) => t.name === 'users');
    const cols = users?.columns.map((c) => `${c.name}:${c.scalar}`) ?? [];
    // Short name "Email" rewritten by link to base.core.Email; single-field
    // value_type with field name "value" → 1 column with no suffix.
    expect(cols).toContain('email:string');
  });

  it('expands a multi-field value_type ref to N prefixed columns', async () => {
    const model = await expandFromStringMap({
      'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - { name: bigint, description: i, properties: [] }
  - name: decimal
    description: d
    properties:
      - { name: precision, type: integer, required: true }
      - { name: scale, type: integer, required: true }
  - name: string
    description: s
    properties:
      - { name: max_length, type: integer, required: true }
`,
      'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
`,
      'systems/base/core/value_type/money.yaml': `version: loom-schema/v2
kind: value_type
name: Money
fields:
  - name: amount
    type: decimal
    precision: 18
    scale: 4
  - name: currency_code
    type: string
    max_length: 3
`,
      'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
using:
  - base.core.*
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: bigint, required: true }
  - { name: balance, type: Money }
primary_key: [id]
`,
    });

    const users = model.tables.find((t) => t.name === 'users');
    const colNames = users?.columns.map((c) => c.name).sort() ?? [];
    expect(colNames).toEqual(['balance_amount', 'balance_currency_code', 'id']);
  });
});
```

- [ ] **Step 3: 跑测试验证失败**

Run: `pnpm --filter @loom/core test -- projector-expand.test.ts`
Expected: 新测试 FAIL（expandField 还在找 `base`/`ref`）。

- [ ] **Step 4: 重写 expandField**

修改 `packages/core/src/projector/expand.ts`。把整个 `function expandField(...)`（行 206-280）替换为：

```typescript
/**
 * Expand a single field into one or more PhysicalColumns (spec v2 §3, §5.5).
 *
 *   - single-segment type (e.g. "integer")  → 1 column, scalar = type value
 *   - three-segment type (value_type fqn):
 *       - single-field newtype (inner field named "value") → 1 column,
 *         scalar = inner field's type
 *       - multi-field → N columns named <prefix>_<subfield>,
 *         scalar = each subfield's type
 *
 * Also records enumRef when the resolved scalar is an enum.
 */
function expandField(
  f: Record<string, unknown>,
  ir: IR,
  enums: Map<string, ReadonlyArray<string>>,
): PhysicalColumn[] {
  const fName = String(f.name);
  const typeVal = String(f.type);

  // Single-segment: direct base_types scalar.
  if (!typeVal.includes('.')) {
    const result: PhysicalColumn = {
      name: fName,
      scalar: typeVal,
      required: f.required === true,
      unique: f.unique === true,
      props: extractProperties(f),
    };
    return [result];
  }

  // Three-segment: value_type reference. The link pass has already verified
  // the node exists and kind === 'value_type', and rewritten short-name
  // matches to their fqn. So typeVal is now `sys.mod.Name`.
  const targetId = `value_type:${typeVal}`;
  const vtNode = ir.nodes.get(targetId);
  if (!vtNode) {
    throw new Error(`Value type not found: ${targetId}`);
  }
  if (vtNode.kind !== 'value_type') {
    throw new Error(`Expected value_type, got ${vtNode.kind} for ${targetId}`);
  }

  const vt = vtNode.data as ValueType;
  const vtNodeForField = vt as unknown as ValueTypeNode;

  if (isSingleFieldValueType(vtNodeForField)) {
    const inner = vt.fields[0] as Record<string, unknown>;
    const scalar = String(inner.type);
    const props = extractProperties(inner);
    let enumRef: string | undefined;
    if (scalar === 'enum' && Array.isArray(inner.values)) {
      enumRef = typeVal;
    }
    const result: PhysicalColumn = {
      name: fName,
      scalar,
      required: f.required === true,
      unique: f.unique === true,
      props,
      ...(enumRef !== undefined ? { enumRef } : {}),
    };
    return [result];
  }

  // Multi-field value_type: one column per subfield.
  const expanded = expandValueColumns(fName, vtNodeForField);
  return expanded.map((col, idx) => {
    const subField = vt.fields[idx] as Record<string, unknown>;
    const subType = String(subField.type);
    const result: PhysicalColumn = {
      name: col.name,
      scalar: subType,
      required: f.required === true,
      unique: f.unique === true,
      props: extractProperties(subField),
      ...(subType === 'enum' && Array.isArray(subField.values) ? { enumRef: typeVal } : {}),
    };
    return result;
  });
}
```

**关键变化**：
- 不再检查 `'base' in f` / `'ref' in f`，统一看 `f.type`
- 单段名（无点）= 直接标量
- 三段名（有点）= value_type 引用，targetId 构造为 `value_type:${typeVal}`
- inner/subfield 的 `base` 引用全改为 `type`

**注意 required/unique 的归属**：v1 对单字段 value_type，required/unique 取**外层字段**（引用处），props 取 inner field。v2 保持这个语义不变（required/unique 来自 `f`，props 来自 `inner`/`subField`）。

- [ ] **Step 5: 改 extractProperties 的 skipKeys**

修改 `extractProperties`（行 287 起）。把 skipKeys 集合里的 `'base'` 和 `'ref'` 替换为 `'type'`。

把：
```typescript
const skipKeys = new Set([
  'name',
  'base',
  'ref',
  'required',
  'unique',
  'default',
  'include',
  'default_scope',
]);
```

改为：
```typescript
const skipKeys = new Set([
  'name',
  'type',
  'required',
  'unique',
  'default',
  'include',
  'default_scope',
]);
```

- [ ] **Step 6: 改 collectEnums 和 collectExtensionFields 里的 base→type**

在 expand.ts 里搜索所有 `.base` 引用。

`collectEnums`（行 314 起）：

把：
```typescript
const first = vt.fields[0] as Record<string, unknown>;
if (first.base === 'enum' && Array.isArray(first.values)) {
  enums.set(identity, first.values as ReadonlyArray<string>);
}
```

改为：
```typescript
const first = vt.fields[0] as Record<string, unknown>;
if (first.type === 'enum' && Array.isArray(first.values)) {
  enums.set(identity, first.values as ReadonlyArray<string>);
}
```

`collectExtensionFields`（行 334 起）—— 这个函数处理 extension_fields 的字段，v1 它区分 `'ref' in fRec` 和 `'base' in fRec`。v2 只有 `type`。把：

```typescript
// Handle ref fields
if ('ref' in fRec) {
  const entry: ExtensionFieldEntry = {
    name: String(fRec.name),
    scalar: '',
    refValueTypeId: String(fRec.ref),
    props: extractProperties(fRec),
    ...(fRec.default_scope !== undefined ? { defaultScope: String(fRec.default_scope) } : {}),
  };
  entries.push(entry);
} else if ('base' in fRec) {
  const entry: ExtensionFieldEntry = {
    name: String(fRec.name),
    scalar: String(fRec.base),
    props: extractProperties(fRec),
    ...(fRec.default_scope !== undefined ? { defaultScope: String(fRec.default_scope) } : {}),
  };
  entries.push(entry);
} else {
  throw new Error(`Extension field must have 'base' or 'ref': ${JSON.stringify(fRec)}`);
}
```

改为：

```typescript
const typeVal = String(fRec.type);
if (typeVal.includes('.')) {
  // Three-segment: value_type reference.
  const entry: ExtensionFieldEntry = {
    name: String(fRec.name),
    scalar: '',
    refValueTypeId: `value_type:${typeVal}`,
    props: extractProperties(fRec),
    ...(fRec.default_scope !== undefined ? { defaultScope: String(fRec.default_scope) } : {}),
  };
  entries.push(entry);
} else {
  // Single-segment: direct base_types scalar.
  const entry: ExtensionFieldEntry = {
    name: String(fRec.name),
    scalar: typeVal,
    props: extractProperties(fRec),
    ...(fRec.default_scope !== undefined ? { defaultScope: String(fRec.default_scope) } : {}),
  };
  entries.push(entry);
}
```

- [ ] **Step 7: 改 resolveFields / expandFields 的类型签名（如果它们用了 base/ref）**

`resolveFields`（行 147）和 `expandFields`（行 184）处理字段数组，不直接看 base/ref，只递归 + 转发。不需要改。

但 `resolveFields` 里的 mixin include 检测（`'include' in fRec`）不变，正确。

- [ ] **Step 8: 跑测试验证通过**

Run: `pnpm --filter @loom/core test -- projector-expand.test.ts`
Expected: 3 条新测试 PASS。

- [ ] **Step 9: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 10: 提交**

```bash
git add packages/core/src/projector/expand.ts packages/core/tests/projector-expand.test.ts
git commit -m "feat(v2): projector expandField — single type: branch

Drops the 'base' in f / 'ref' in f split. Now:
  - single-segment type (no dot) → 1 column, scalar = type value
  - three-segment type (value_type fqn) → 1 or N columns per §5.5

extractProperties skipKeys swaps base/ref → type. collectEnums and
collectExtensionFields updated to read field.type and classify by
dot presence (three-seg = value_type ref, single-seg = scalar).

Three new tests: direct scalar, single-field value_type via using
short name (rewritten by link), multi-field value_type prefixed cols."
```

---

## Task 10: 测试固件迁移到 v2 语法（base_schema.ts）

**Files:**
- Modify: `packages/core/tests/fixtures/base_schema.ts`

**背景：** 这是被大量测试引用的核心固件。把它从 v1 语法迁移到 v2。函数签名 `buildBaseSchemaFs(): FileSystem` 不变，只改内部 YAML 字符串。

迁移规则：
- `version: loom-schema/v1` → `version: loom-schema/v2`
- 字段 `- name: x\n    base: y` → `- name: x\n    type: y`
- 字段 `ref: value_type:base.core.Email` → `type: base.core.Email`（或经 using 短名 `Email`，但固件用全限定更清晰）
- mixin include `- include: mixin:base._shared.Audit` 不变（spec §5）
- 给引用了 value_type 的文件加 `using:` 或直接用全限定名。**选择全限定名**（固件不应隐含 using 行为，要显式）。

- [ ] **Step 1: 改 base_schema.ts**

把 `packages/core/tests/fixtures/base_schema.ts` 整个 `buildBaseSchemaFs` 函数体里的 YAML 字符串替换。

完整新版本（注意：所有 `version:` 改 v2；所有字段 `base:` 改 `type:`；`ref:` 改 `type:` + 全限定名；mixin include 不变）：

```typescript
import type { FileSystem } from '../../src/loader/fs.js';
import { MemoryFileSystem } from './memory_fs.js';

/**
 * A small but complete loom schema used across loader/projector tests.
 * v2 syntax: single type: key, no base/ref split, no kind prefix on type
 * refs. Covers: base_types, module_manifest, single & multi value_types,
 * mixin include, sidecar_eav table, entity referencing the table,
 * extension_fields targeting the entity.
 */
export function buildBaseSchemaFs(): FileSystem {
  return new MemoryFileSystem({
    'base_types.yaml': `version: loom-schema/v2
kind: base_types
scalars:
  - name: bigint
    description: 64-bit integer
    properties: []
  - name: decimal
    description: fixed-point
    properties:
      - name: precision
        type: integer
        required: true
      - name: scale
        type: integer
        required: true
  - name: string
    description: var-length string
    properties:
      - name: max_length
        type: integer
        required: true
      - name: pattern
        type: string
  - name: datetime
    description: timestamp
    properties: []
  - name: boolean
    description: boolean
    properties: []
`,
    'systems/base/core/MANIFEST.yaml': `version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
description: core module
`,
    'systems/base/_shared/mixin/audit.yaml': `version: loom-schema/v2
kind: mixin
name: Audit
fields:
  - name: created_at
    type: datetime
    required: true
  - name: updated_at
    type: datetime
    required: true
`,
    'systems/base/core/value_type/email.yaml': `version: loom-schema/v2
kind: value_type
name: Email
fields:
  - name: value
    type: string
    max_length: 254
`,
    'systems/base/core/value_type/money.yaml': `version: loom-schema/v2
kind: value_type
name: Money
fields:
  - name: amount
    type: decimal
    precision: 18
    scale: 4
    required: true
  - name: currency_code
    type: string
    max_length: 3
    required: true
`,
    'systems/base/core/table/users.yaml': `version: loom-schema/v2
kind: table
name: Users
table:
  name: users_base
  extension:
    strategy: sidecar_eav
    ext_table: users_ext
    view: users
fields:
  - name: id
    type: bigint
    required: true
  - include: mixin:base._shared.Audit
  - name: email
    type: base.core.Email
    required: true
    unique: true
  - name: balance
    type: base.core.Money
primary_key: [id]
indexes:
  - name: idx_users_email
    fields: [email]
    unique: true
`,
    'systems/base/core/entity/user.yaml': `version: loom-schema/v2
kind: entity
name: User
primary_table: table:base.core.Users
business_keys: [email]
audit: true
`,
    'systems/base/core/extension/user_fields.yaml': `version: loom-schema/v2
kind: extension_fields
entity: entity:base.core.User
fields:
  - name: nickname
    type: string
    max_length: 50
    default_scope: tenant
`,
  });
}
```

注意：table/users.yaml 里 `type: base.core.Email` 和 `type: base.core.Money` 用全限定名，没加 using（固件显式表达，不依赖隐式行为）。

- [ ] **Step 2: 跑依赖此固件的测试**

Run: `pnpm --filter @loom/core test -- loader.test.ts`
Expected: loader.test.ts 如果用了 buildBaseSchemaFs，应该开始通过（如果它之前因 v1 语法失败）。如果还失败，记录失败信息。

Run: `pnpm --filter @loom/core test -- fixtures.test.ts`
Expected: PASS（这个测试大概是验证固件结构）。

- [ ] **Step 3: 跑 tsc check**

Run: `pnpm --filter @loom/core check`
Expected: 无类型错误。

- [ ] **Step 4: 提交**

```bash
git add packages/core/tests/fixtures/base_schema.ts
git commit -m "feat(v2): migrate base_schema.ts fixture to v2 syntax

All YAML strings updated: version v1→v2, field base:→type:, ref:
value_type:... → type: <fqn> (no kind prefix). Mixin include syntax
unchanged (spec §5 defers mixin using-ification). Table uses fully-
qualified type names directly rather than via using, to keep the fixture
explicit about what it exercises."
```

---

## Task 11: 所有 *.test.ts 迁移到 v2 语法

**Files:**
- Modify: 所有 `packages/core/tests/*.test.ts` 里硬编码 v1 YAML 的地方
- Modify: 所有 `packages/cli/tests/*.test.ts` 里硬编码 v1 YAML 的地方

**背景：** Task 2-10 已经把核心代码改完，新写的测试都是 v2 语法。但旧的测试文件（除上面任务里改过的）还有大量 v1 语法硬编码 YAML。这个任务把它们全部扫一遍迁移。

**注意**：v1 → v2 的迁移规则已在 Task 10 列出。每个测试文件单独改、单独跑、单独提交（如果一个文件改动大）；如果改动小可以几个文件一起。

- [ ] **Step 1: 扫描所有 v1 残留**

Run this Grep equivalent:

```bash
grep -rn "loom-schema/v1\|base:\|ref: value_type:\|ref: entity:\|ref: mixin:" packages/core/tests packages/cli/tests --include="*.ts" | grep -v "fixtures/base_schema.ts" | grep -v "\.test\.ts.*describe\|it("
```

或用 Grep tool：搜 `loom-schema/v1`，然后搜 `ref: value_type:`，然后搜字段级的 `base:`（排除 base_types.yaml 文件内容里的 `name: base_types` 等）。

记录每个文件的残留行数。

- [ ] **Step 2: 逐文件迁移**

对每个有残留的测试文件：

a. 把所有 `version: loom-schema/v1` 改为 `version: loom-schema/v2`
b. 把字段 `- name: x\n    base: y` 改为 `- name: x\n    type: y`（注意 YAML 缩进）
c. 把字段 `ref: value_type:base.core.X` 改为 `type: base.core.X`（或加 using + 短名，但保持原有风格——如果原测试就用全限定 ref，改成全限定 type）
d. `ref: entity:...` / `ref: mixin:...` 如果出现在 fields 数组里（不是 include/primary_table/ref_table），改为对应的全限定 type——但实际上 v1 fields 里只会有 `ref: value_type:`，entity/mixin ref 出现在 primary_table/ref_table/include 这些**身份 ref** 位置，那些**不变**（身份 ref 保持 kind 前缀）

**关键区分**：
- `fields:` 内的 `ref:` → 改 `type:` + 去掉 kind 前缀
- `primary_table:` / `ref_table:` / `include:` 的值 → **不变**（这些是身份 ref，带 kind 前缀）

- [ ] **Step 3: 每改完一个文件就跑该文件的测试**

Run: `pnpm --filter @loom/core test -- <file>.test.ts`
Expected: PASS。如果失败，看失败信息——可能是漏改了某处，或者测试断言本身需要更新（比如断言 `field.base` 的要改成断言 `field.type`）。

- [ ] **Step 4: 处理断言变化**

有些测试可能断言 v1 的字段结构，比如 `expect(field.base).toBe('integer')`。这些要改成 `expect(field.type).toBe('integer')`。

搜索：`grep -rn "\.base\b\|\.ref\b" packages/core/tests packages/cli/tests --include="*.ts"`。排除 `.references`、`.referrer` 等无关匹配。把断言 v1 字段结构的改成 v2。

- [ ] **Step 5: 跑全量 core 测试**

Run: `pnpm --filter @loom/core test`
Expected: 所有测试 PASS。如果有失败，逐个修复。

- [ ] **Step 6: 跑全量 cli 测试**

Run: `pnpm --filter @loom/cli test`
Expected: 所有测试 PASS。

- [ ] **Step 7: 跑 tsc check（两个包）**

Run: `pnpm -r run check`
Expected: 无类型错误。

- [ ] **Step 8: 提交**

```bash
git add packages/core/tests packages/cli/tests
git commit -m "test(v2): migrate all remaining test YAML to v2 syntax

Sweeps every test file for v1 residue: version strings, field base:/ref:
keys, assertions on field.base/field.ref. Identity refs (primary_table,
ref_table, mixin include) keep their kind: prefix — only field type
references changed.

All core + cli tests pass; both packages typecheck clean."
```

---

## Task 12: 黄金固件重新生成（base_schema.pg.sql）

**Files:**
- Modify: `packages/core/tests/golden/base_schema.pg.sql`
- Modify: `packages/core/tests/golden.test.ts`（如果它对比的是字节级一致）

**背景：** 黄金固件锁定投影 SQL 输出。v2 的语义和 v1 等价（同样的 schema 投影出同样的 SQL），所以 SQL 内容**应该不变**。但需要重新生成确认，并确保 golden.test.ts 用 v2 固件跑。

- [ ] **Step 1: 看 golden.test.ts 怎么工作**

Run: `cat packages/core/tests/golden.test.ts`
记录它怎么调用 projector、怎么对比。

- [ ] **Step 2: 手动跑一次投影，对比现有黄金**

写一个一次性脚本（不提交）或直接用 cli：

```bash
# 临时：把 base_schema.ts 固件 dump 成文件系统，跑 cli project
# 更简单：写个临时测试打印输出
```

或者更直接：在 golden.test.ts 里临时加一个 `console.log`，跑测试看输出：

修改 golden.test.ts，在断言前临时加：
```typescript
console.log('=== GENERATED SQL ===');
console.log(sql);
console.log('=== END ===');
```

Run: `pnpm --filter @loom/core test -- golden.test.ts`
查看输出。

- [ ] **Step 3: 对比生成结果 vs 现有黄金固件**

对比 Step 2 的输出和 `packages/core/tests/golden/base_schema.pg.sql` 现有内容。

**预期**：语义等价，SQL 应该一致（列名、类型、约束都一样）。如果不一致，记录差异——可能是 v2 改造引入的 bug，需要回头修。

- [ ] **Step 4: 如果有差异，先修 bug**

如果生成的 SQL 和黄金不一致（且不是预期的改进），定位是 expand.ts 还是 dialect 的问题，修复后重新生成。

- [ ] **Step 5: 更新黄金固件（如果语义确实变了）**

如果差异是合理的（比如 v2 改了某个投影规则），更新 `packages/core/tests/golden/base_schema.pg.sql` 为新生成的内容。

删除 Step 2 加的 `console.log`。

- [ ] **Step 6: 跑 golden 测试验证通过**

Run: `pnpm --filter @loom/core test -- golden.test.ts`
Expected: PASS（生成的 SQL 和黄金固件字节级一致）。

- [ ] **Step 7: 跑全量测试**

Run: `pnpm -r run test`
Expected: 所有测试 PASS。

- [ ] **Step 8: 提交**

```bash
git add packages/core/tests/golden/base_schema.pg.sql packages/core/tests/golden.test.ts
git commit -m "test(v2): regenerate golden fixture from v2 schema

The v2 base_schema.ts fixture projects to semantically equivalent SQL.
Golden file updated to match (no semantic drift). If the projection had
diverged, this commit would be the place to explain why."
```

---

## Task 13: cli version 命令输出 v2

**Files:**
- Verify: `packages/cli/src/commands/version.ts`
- Test: `packages/cli/tests/version.test.ts`（如果有）

**背景：** version.ts 引用 `FORMAT_VERSION`（来自 core），Task 1 已经把它改成 v2，所以 cli 的 version 命令输出会自动变成 `loom-schema/v2`。这个任务只是验证。

- [ ] **Step 1: 看 version.ts**

Run: `cat packages/cli/src/commands/version.ts`
确认它引用 `FORMAT_FAMILY` / `FORMAT_VERSION` / `CURRENT_VERSION`。

- [ ] **Step 2: 看 cli version 测试**

Run: `ls packages/cli/tests/ && cat packages/cli/tests/version.test.ts 2>/dev/null || echo "no version test"`
如果有测试，看它断言什么。

- [ ] **Step 3: 改测试断言（如果有）**

如果 cli 有 version 测试断言 `loom-schema/v1`，改为 `loom-schema/v2`。

- [ ] **Step 4: 重新构建 core（cli 依赖它）**

Run: `pnpm --filter @loom/core build`
Expected: 构建成功。

- [ ] **Step 5: 跑 cli 测试**

Run: `pnpm --filter @loom/cli test`
Expected: 所有测试 PASS。

- [ ] **Step 6: 手动验证 cli 输出**

Run: `node packages/cli/bin/loom version`
Expected 输出：
```
loom 0.1.0
schema-versions-supported: loom-schema/v2
current: loom-schema/v2
```

（CLI_VERSION 还是 0.1.0，Task 15 会 bump 到 0.2.0）

- [ ] **Step 7: 提交（如果有改动）**

如果改了测试断言：
```bash
git add packages/cli/tests/version.test.ts
git commit -m "test(cli): version command expects loom-schema/v2

FORMAT_VERSION bump in core propagates automatically; this just updates
the cli test assertion."
```

如果没改动（没测试或测试已对），跳过提交，记录"无需改动"。

---

## Task 14: USER_GUIDE + README 更新

**Files:**
- Modify: `docs/USER_GUIDE.md`（第 5/10/11 章 + 附录 A）
- Modify: `README.md`（Status 段）

**背景：** USER_GUIDE 是 v1 语法写的。v2 语法变了，需要更新。重点章节：

- 第 5 章 value_type：`ref: value_type:...` → `type: <fqn>`，加 using 说明
- 第 10 章 enum：取消 inline enum，强制 value_type；加 using 例子
- 第 11 章 `$ref`：类型 ref 不带 kind 前缀；身份 ref（mixin include / primary_table / ref_table）保留 kind 前缀
- 附录 A 完整示例：全部 v1 → v2
- 新增一小节关于 using 导入机制

README 的 Status 段从 v0.1.0 改 v0.2.0，列出 v2 的核心变化。

- [ ] **Step 1: 改 USER_GUIDE 第 5 章 value_type**

打开 `docs/USER_GUIDE.md`，定位"第 5 章 value_type：语义类型包装"。

把 5.1 单字段 value_type 的引用示例：
```yaml
fields:
  - name: email
    ref: value_type:base.core.Email
```
改为：
```yaml
fields:
  - name: email
    type: base.core.Email     # 或经 using 短名: type: Email
```

5.2 多字段同理，`ref: value_type:base.core.Money` → `type: base.core.Money`。

5.4 base/ref 互斥那一节整段重写为"v2: 单一 type: 键"，说明单段 vs 三段。

- [ ] **Step 2: 在第 5 章后新增"第 5.5 章 using 导入机制"**

插入新章节，内容覆盖：
- 文件级 using
- 默认 `base.core.*`
- A（`ns.*`）与 B（`ns.Name`）统一语法
- 短名解析规则
- 歧义检测
- 形态 C（重命名）挂起

参考 spec §4 的表述，但用更教学的口吻（USER_GUIDE 是教程不是 spec）。

- [ ] **Step 3: 改第 10 章 enum**

把 10.1 的 inline enum 例子删除或标注"v2 已取消"。改为只展示 value_type 定义 + type 引用。

把：
```yaml
fields:
  - name: status
    base: enum
    values: [active, inactive, suspended]
```
替换为 value_type 文件 + 引用：
```yaml
# value_type/user_status.yaml
fields:
  - name: value
    type: enum
    values: [active, inactive, suspended]

# 引用处
fields:
  - name: status
    type: base.core.UserStatus
```

- [ ] **Step 4: 改第 11 章 `$ref` 引用语法**

把"跨文件引用是字符串"那节分成两个小节：

**11.1 类型引用（field type:）**：三段名 `sys.mod.Name`，无 kind 前缀。短名经 using。

**11.2 身份引用（非类型）**：`kind:sys.mod.Name`，带 kind 前缀。用于：
- `primary_table:`
- `ref_table:`（foreign_keys）
- `- include: mixin:...`
- `entity:`（extension_fields 的 entity 字段）

强调两者的区别和为什么这么设计（类型名字空间 vs 身份名字空间）。

- [ ] **Step 5: 改附录 A 完整示例**

附录 A 的所有 YAML 改 v2 语法（和 Task 10 的 base_schema.ts 对应）。全部 `version: v2`、字段 `base:` → `type:`、`ref:` → `type:`。

附录 A.8 的投影 SQL 不变（语义等价）。

- [ ] **Step 6: 改 README Status 段**

把 `README.md` 的：
```
## Status

v0.1.0 — `check` + `project sql` for PG/MySQL/SQLite.
`fmt`, `lift`, `project atlas-yaml` are follow-up work.
```

改为：
```
## Status

v0.2.0 — unified type system (`type:` + `using` imports), `check` +
`project sql` for PG/MySQL/SQLite.

v0.1.0 used `base`/`ref` field keys (v1 wire format); v0.2.0 migrates
to a single `type:` key with a `using` import mechanism (v2 wire format).
See `docs/specs/2026-06-18-loom-v2-type-system.md`.

`fmt`, `lift`, `project atlas-yaml` are still follow-up work.
```

- [ ] **Step 7: 跑测试 + lint（文档改动不影响测试，但确认没破东西）**

Run: `pnpm -r run test && pnpm lint`
Expected: 全部 PASS / lint 清洁。

- [ ] **Step 8: 提交**

```bash
git add docs/USER_GUIDE.md README.md
git commit -m "docs(v2): update USER_GUIDE and README for v2 type system

USER_GUIDE chapters 5 (value_type), 10 (enum), 11 (\$ref) rewritten for
the single type: key + using imports. New §5.5 covers the using mechanism
(file-level, default base.core.*, A/B unified syntax, form C deferred).
Appendix A fully migrated to v2 syntax; projected SQL unchanged.

README status bumped v0.1.0 → v0.2.0 with a one-line summary of the
v1→v2 migration and a pointer to the v2 spec."
```

---

## Task 15: 全量 lint + 完整测试 + 最终提交 + 标签 v0.2.0

**Files:**
- All packages

**背景：** 最后收尾。跑所有质量门，bump 版本号到 v0.2.0，打标签。

- [ ] **Step 1: 全量 lint**

Run: `pnpm lint`
Expected: 清洁。如果有 lint 错误（比如 noNonNullAssertion、noUnusedTemplateLiteral），用 `pnpm lint --fix` 或手动修复。

- [ ] **Step 2: 全量类型检查**

Run: `pnpm -r run check`
Expected: 两个包都无类型错误。

- [ ] **Step 3: 全量构建**

Run: `pnpm -r run build`
Expected: 两个包都构建成功。

- [ ] **Step 4: 全量测试**

Run: `pnpm -r run test`
Expected: 所有测试 PASS。记录测试数量（应该和 v1 的 76 个相当，加上 v2 新增的 typespace/using 测试会更多）。

- [ ] **Step 5: 手动冒烟测试 cli**

```bash
# version
node packages/cli/bin/loom version
# 期望输出含 loom-schema/v2

# 写一个临时 v2 schema 跑 project
mkdir -p /tmp/loom-smoke/systems/base/core/{table,value_type}
cat > /tmp/loom-smoke/base_types.yaml <<'EOF'
version: loom-schema/v2
kind: base_types
scalars:
  - { name: bigint, description: i, properties: [] }
  - name: string
    description: s
    properties:
      - { name: max_length, type: integer, required: true }
EOF
cat > /tmp/loom-smoke/systems/base/core/MANIFEST.yaml <<'EOF'
version: loom-schema/v2
kind: module_manifest
system: base
module: core
physical_schema: base_core
EOF
cat > /tmp/loom-smoke/systems/base/core/table/users.yaml <<'EOF'
version: loom-schema/v2
kind: table
name: Users
table:
  name: users
  extension: { strategy: none }
fields:
  - { name: id, type: bigint, required: true }
  - name: email
    type: string
    max_length: 254
primary_key: [id]
EOF

node packages/cli/bin/loom check /tmp/loom-smoke
# 期望: 无输出，退出码 0

node packages/cli/bin/loom project sql --dialect pg /tmp/loom-smoke
# 期望: CREATE TABLE base_core.users (...)

rm -rf /tmp/loom-smoke
```

- [ ] **Step 6: bump CLI 版本号**

修改 `packages/cli/src/commands/version.ts`：

把
```typescript
export const CLI_VERSION = '0.1.0' as const;
```
改为
```typescript
export const CLI_VERSION = '0.2.0' as const;
```

- [ ] **Step 7: 跑 cli 测试确认版本号**

Run: `pnpm --filter @loom/cli test`
Expected: PASS（如果有断言 CLI_VERSION 的测试，可能需要更新）。

- [ ] **Step 8: 最终提交**

```bash
git add packages/cli/src/commands/version.ts
git commit -m "chore: bump to 0.2.0 (loom v2 type system)

v2 wire format: single type: key + using imports. See
docs/specs/2026-06-18-loom-v2-type-system.md.

All tests pass, lint clean, both packages build."
```

- [ ] **Step 9: 打标签**

```bash
git tag -a v0.2.0 -m "loom v0.2.0 — unified type system (type: + using)

v2 wire format. Breaking change from v0.1.0 (loom-schema/v1 → v2).
Field base/ref mutex replaced by single type: key. Type namespace
separated from identity namespace. File-level using imports with
default base.core.*. Inline enum banned; enum must go through value_type."
```

- [ ] **Step 10: 验证标签 + 总结**

Run: `git tag -l 'v0.*' && git log --oneline feat/v2-type-system | head -20`
Expected: 看到 v0.1.0 和 v0.2.0 两个标签，v2 分支的提交历史。

向用户报告完成状态：测试数、lint 状态、标签、已知局限（沿用 v1 的：诊断无行号、FK entity ref 不解析、短格式身份 ref、fmt/lift/atlas-yaml）+ v2 新增的挂起项（形态 C 重命名、mixin using 化）。

---

## Self-Review

（plan 作者在写完后做的检查，记录在此供执行者参考）

### Spec coverage

| Spec 章节 | 实现任务 |
|---|---|
| §3 字段 `type:` 语法 | Task 2 (schema), Task 6 (link), Task 7 (validate), Task 9 (projector) |
| §3.3 错误情况 | Task 6 (unknown type, kind_mismatch), Task 7 (unknown scalar) |
| §4 using 机制 | Task 4 (schema), Task 5 (resolveShortName), Task 6 (link 集成) |
| §4.4 A/B 统一语法 | Task 4 (Zod 接受两种形态), Task 5 (resolveShortName 处理 `.*` 和精确) |
| §4.5 形态 C 挂起 | 不实现，spec §10 记录 |
| §4.6 名字解析规则 | Task 5 (resolveShortName 完整规则) |
| §6 enum 强制 value_type | Task 8 (inline enum 检测) |
| §7.2 类型 vs 身份名字空间 | Task 3 (parseTypeRef), Task 6 (link 分流), Task 9 (projector 分流) |
| §8 version bump v2 | Task 1 |
| §9.3 测试固件迁移 | Task 10, 11, 12 |
| §9.4 文档更新 | Task 14 |

### 挂起项（v2 不实现，spec §10 记录）

- 形态 C（重命名）语法
- mixin using 化（spec §5）

### 已知 v1 局限沿用（v2 不解决）

- 诊断无 line:col（仍 1:1）
- FK ref_table 原样渲染（entity ref 不解析为 primary_table）
- 短格式身份 ref（`kind:.Name`）未实现
- `fmt` / `lift` / `project atlas-yaml` 命令未实现

### Placeholder scan

无 TBD/TODO/"implement later"。每个 step 都有具体代码或具体命令。

### Type consistency

- `TypeRef { system, module, name }` — Task 3 定义，Task 6 使用，一致
- `ResolutionResult` 四种 kind — Task 5 定义，Task 6 使用，一致
- `checkTypedField(identity, hostKind, f, scalarNames, requiredProps, diag)` — Task 7/8 定义和调用点签名一致
- `expandField(f, ir, enums): PhysicalColumn[]` — Task 9 签名不变（只改内部逻辑）
- `resolveShortName(shortName, using, scalars, valueTypes)` — Task 5 定义，Task 6 调用，参数顺序一致

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-18-loom-v2.md`. Two execution options:

**1. Subagent-Driven (recommended)** - 每个任务分派新的 implementer 子代理 + 规范审查 + 代码质量审查，任务间快速迭代

**2. Inline Execution** - 在当前会话用 executing-plans 批量执行，带检查点

哪种？
